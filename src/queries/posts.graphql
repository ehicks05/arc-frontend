fragment PostFragment on Post {
  id
  title
  link
  createdAt
  updatedAt
  author {
    id
    username
  }
  commentCount
  netVotes
  score
}

query GetPosts($sort: Sort) {
  getPosts(sort: $sort) {
    ...PostFragment
  }
}

query GetPostById($id: ID) {
  getPostById(id: $id) {
    ...PostFragment
    comments {
      ...CommentFragment
    }
  }
}
